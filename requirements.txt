--find-links https://download.pytorch.org/whl/cu111/torch_stable.html
# Use python 3.8
numpy==1.24.3
scipy==1.10.1
pandas==2.0.3
scikit-learn==1.3.2
sktime==0.29.1
matplotlib==3.7.5
tqdm==4.67.3
rich==14.3.4
hydra-core==1.3.2
munch==4.0.0
omegaconf==2.3.0
wandb==0.24.2
einops==0.8.1
opt_einsum==3.4.0
# PyKeOps 1.5 is not compatible with CMake 4.x.
cmake==3.31.10
datasets==3.1.0
transformers==4.46.3
pytorch-fast-transformers==0.4.0

# Pin the old training stack used by this repository.
# The post0 Lightning release fixes metadata that newer pip versions reject.
pytorch-lightning==1.5.10.post0
torch==1.10.0+cu111
torchvision==0.11.0+cu111
torchaudio==0.10.0+cu111
torchtext==0.11.0
torchmetrics==1.5.2

# PyKeOps enables the fast Cauchy kernel path.
# For GPU kernels, the environment also needs a local CUDA compiler:
#   conda install -c conda-forge cudatoolkit-dev=11.1.1
# CUDA 11.1 nvcc requires a GCC/G++ host compiler <= 10, e.g. gcc-9 and g++-9.
pykeops==1.5
