# FLA-enabled environment matching the current `/data/users/jianchen/venv/lra` stack.
# Recommended Python: 3.10.x
# Notes:
# - This file tracks the FLA-capable environment, not the legacy `torch==1.10` stack in `requirements.txt`.
# - `flash-linear-attention` is pinned to the exact source snapshot currently installed.
# - `torchtext` is intentionally omitted; the repo now has a cache fallback for LRA text datasets.

--extra-index-url https://download.pytorch.org/whl/cu128

# Core scientific stack
numpy==1.24.3
scipy==1.10.1
pandas==2.0.3
scikit-learn==1.3.2
matplotlib==3.7.5
tqdm==4.67.3
rich==14.3.4

# Configuration / utilities
hydra-core==1.3.2
munch==4.0.0
omegaconf==2.3.0
einops==0.8.1
opt_einsum==3.4.0
cmake==4.3.2
ninja==1.13.0

# Hugging Face / data tooling
datasets==3.1.0
transformers==4.46.3
tokenizers==0.20.3
huggingface_hub==0.36.2
safetensors==0.7.0

# Training / logging
wandb==0.24.2
pytorch-lightning==1.5.10.post0
torchmetrics==1.5.2

# PyTorch CUDA 12.8 stack
torch==2.7.1+cu128
torchvision==0.22.1+cu128
torchaudio==2.7.1+cu128
triton==3.3.1

# Flash Linear Attention
flash-linear-attention @ git+https://github.com/fla-org/flash-linear-attention.git@71ebc4981c6ab42d66a7aafd075d4396c3cf407d
