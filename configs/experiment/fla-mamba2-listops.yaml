# @package _global_
defaults:
  - /pipeline: listops
  - /model: hf_fla_mamba2
  - override /scheduler: cosine_warmup

encoder: hf

model:
  config_kwargs:
    num_hidden_layers: 8
    state_size: 16
    head_dim: 16
    hidden_size: 16

decoder:
  mode: pool

loader:
  batch_size: 50

optimizer:
  lr: 0.01
  weight_decay: 0.05

scheduler:
  num_training_steps: 80000
  # patience: 5

trainer:
  max_epochs: 40

train:
  seed: 2222
